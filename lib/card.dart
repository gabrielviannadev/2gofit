import 'package:flutter/material.dart';

// Preferi criar um arquivo .dart novo apenas para alojar os cartões, que utilizei diversas vezes na tela, com o método Stateful.
class card2go extends StatefulWidget {
  const card2go({Key? key}) : super(key: key);

  @override
  State<card2go> createState() => _cardState();
}

class _cardState extends State<card2go> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
